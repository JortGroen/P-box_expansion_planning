# Experiments

Every experiment must run from a version-controlled config and write a
`manifest.json`. The bootstrap run in `experiments/bootstrap` is non-scientific
and exists only to verify the manifest path.

