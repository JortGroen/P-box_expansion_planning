# ASSUMPTIONS.md

Assumptions proposed by agents remain unsigned until the PI signs them. Every
row must have a same-ID standalone paragraph block in
`paper/methods_decisions_and_assumptions.md`, with its proposed, approved,
superseded, or not-invoked status stated explicitly.

| ID | Date | Statement | Rationale | Source/evidence | Owner | Status | PI sign-off |
|---|---|---|---|---|---|---|---|
| A-001 | 2026-07-08 | Candidate primary P_crit is 1e-2 with 1e-3 sensitivity. | Matches the project plan, but must be frozen at G0. | `project_plan_v3_when_can_grid_reinforcement_wait.md` section 8E | HUMAN | proposed | -- |
| A-002 | 2026-07-08 | Candidate alpha grid is {0, 0.25, 0.5, 0.75, 1.0}. | Matches the project plan and agent guardrails, but must be frozen at G0/G1. | `project_plan_v3_when_can_grid_reinforcement_wait.md` section 3 | HUMAN | proposed | -- |
| A-003 | 2026-07-08 | Primary grid candidates are SimBench MV semi-urban and urban, with CIGRE MV as cross-check. | Matches the resolved design plan; final selection belongs to G0. | `project_plan_v3_when_can_grid_reinforcement_wait.md` section 8A | HUMAN | proposed | -- |
| A-004 | 2026-07-08 | Report alpha-indexed lower/upper probability bounds and MC CIs; never defuzzify to one scalar. | Hard reporting rule in the project plan and agent instructions. | `project_plan_v3_when_can_grid_reinforcement_wait.md` section 3; `agent_instructions.md` section 1 | all | proposed | -- |
| A-005 | 2026-07-09 | Parallel decision-transformer units are treated as one substation decision asset with aggregate apparent-power magnitude `abs(sum_i S_i(t)) / sum_i S_nom_i`, where `S_i(t)` is complex apparent power through unit `i`. This is equivalent to per-unit loading only under busbar-parallel operation with closed tie, equal taps, identical units, and no material circulating-current effects. If E1.S1 shows separate MV sections or open-tie operation, the decision asset must be redefined and escalated. | Matches the G0/G0-A1 decision-transformer aggregation rule and Tier-1 net-load summation evaluator. The magnitude is still direction-agnostic for screening/export reporting, while the primary event is conditioned on import direction. Avoids pretending the fast tier has per-unit split or circulating-current information. | G0 scope freeze; G0-A1; E1.S1 inventory follow-up must record element index(es), unit count, bus-tie configuration, and nameplates; G2 must compare aggregate loading against pandapower per-unit `res_trafo.loading_percent`. | A | proposed | -- |
| A-006 | 2026-07-09 | Constant nodal power factor supplies Q for `abs(S)` in Tier-1. The flexibility aggregator adjusts P, with Q following the assigned power factor. | The G0-A1 primary event uses apparent-power magnitude conditioned on import direction, while Tier-1 computes `S_net` from aggregated downstream net P and Q. This assumption makes the Q treatment explicit until later validation. | G0 scope freeze; G0-A1; E1.S4 Tier-1 evaluator; G2 summation-vs-AC comparison. | A | proposed | -- |
| A-007 | 2026-07-09 | Dutch KNMI weather drives German-measured SimBench baseline profiles. This is justified because heating load is modeled separately through the heat-pump layer, leaving baseline demand weakly weather-coupled. | G0 uses Dutch weather to keep adoption/weather/profile assumptions coherent with the Dutch scenario layer, while preserving validated SimBench topology and baseline profiles. | G0 scope freeze; E2.S1 data acquisition; E2.S3 heat-pump model. | C | proposed | -- |
| A-008 | 2026-07-09 | Fallback screen thresholds `L_base > 0.85 p.u.` and `L_2035^(rho=0) < 0.95 p.u.` are routing heuristics for grid selection, not scientific claims. | The thresholds decide whether the primary grid is too already-congested or too unthreatened to answer the deferral question. They must not be reported as planning standards or tuned silently for case interestingness. | G0 scope freeze; deterministic screen before Monte Carlo; G5 case selection. | A | proposed | -- |
| A-009 | 2026-07-10 | Earlier proposal: use the 2035 behavior library for 2033 if the API rejects native `simulated_year = 2033`. | Superseded because EV-004 fixes one 2030 residential charge-point behavior distribution for every planning layer; no year-specific residential behavior fallback is required. | `reports/elaad_profile_generation_spec.md`; EV-004. | C | superseded | Superseded by PI-approved EV-004, 2026-07-16 |
| A-010 | 2026-07-10 | Earlier combined proposal: use 11 kW for home charging and 22 kW for public charging, with a possible 7.4 kW home sensitivity. | Superseded as a combined row. EV-004 approves 11 kW only for the fixed home charge-point class; the public charging capacity remains unresolved and may not be inferred from this row. | `reports/elaad_profile_generation_spec.md`; EV-004. | C | superseded | Home portion replaced by PI-approved EV-004, 2026-07-16; public portion remains open |
| A-011 | 2026-07-10 | Earlier proposal: represent home demand with ElaadNL `ev` profiles and scale by home-access EV count. | Superseded because EV-004 changes the residential sampling unit to one physical home charge point represented by a `cp` profile; E2.S6 must consequently provide home charge-point counts rather than home-access EV counts. | `reports/elaad_profile_generation_spec.md`; EV-004; E2.S6. | C | superseded | Superseded by PI-approved EV-004, 2026-07-16 |
| A-012 | 2026-07-10 | Sign convention for event direction: `P_net > 0` denotes net import from the upstream grid into the MV area; `P_net < 0` denotes net export/feed-in from the MV area to the upstream grid; `P_net = 0` belongs to neither direction and is captured only by the unconditioned screening metric. Import and export loading both use apparent-power magnitude `abs(S_net) / S_nom,agg` after direction conditioning. A direction flip resets the primary overload-episode counter. | G0-A1 scopes the primary event to consumption-driven congestion while keeping export/feed-in loading visible as a side metric. The explicit sign convention prevents active-power-only loading or signed-import mistakes. | G0-A1; E1.S3 reverse-PV screen finding; E1.S4 evaluator implementation. | A | proposed | -- |
| A-013 | 2026-07-14 | At the DSO model boundary, represent grid-model output discrepancy by a symmetric relative interval `epsilon_grid` with arbitrary unknown dependence on inputs, controllability, time, and Tier-1 error. The candidate reference is 5%, with 2% and 10% sensitivity cases, but these numbers remain author-specified scenarios: they are not empirical bounds, confidence intervals, completed expert elicitation, or presently established by literature. E9.S5a must complete a targeted evidence review before the PI signs the values. The asserted operating domain is frozen from the predeclared manifested E3.S2b future-layer screen before probabilistic-result inspection; later out-of-domain states are flagged and escalated. | G1-A2 freezes a simple, auditable relative form that is invariant to the total-versus-firm denominator choice while preserving honest ignorance about model discrepancy. The explicit proposed status prevents a convenient 5% scenario from being presented as scientifically measured. | G1-A1; G1-A2; `reports/G1_A2_GRID_ERROR_AND_CAPACITY_PROTOCOL.md`; E3.S2b domain manifest; E9.S5a evidence review; E9.S5 sensitivity manifests. | HUMAN | proposed | -- |
| A-014 | 2026-07-20 | Proposed E2.S6 local allocation rule: allocate the D-009 national home and public charge-point counts across the 115 in-service `net.load` rows of SimBench `1-MV-semiurb--0-sw` in proportion to each row's static `p_mw`, then convert fractional counts to nonnegative integers by deterministic largest-remainder rounding with ties by node ID. | Supplies the EV-005 `K_r` cohort-size ranges needed to decide replacement governance while preserving EV-004's fixed behavior library. The rule is transparent and reproducible, but it is a proposed benchmark scaling rule rather than a measured Dutch feeder population. | D-009; D-010 scenario framing; `configs/scenarios.yaml`; `reports/e2_s6_adoption_scenarios.md`; Q-7. | C | proposed | -- |
