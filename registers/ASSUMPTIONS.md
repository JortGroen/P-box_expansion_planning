# ASSUMPTIONS.md

Assumptions proposed by agents remain unsigned until the PI signs them.

| ID | Date | Statement | Rationale | Source/evidence | Owner | Status | PI sign-off |
|---|---|---|---|---|---|---|---|
| A-001 | 2026-07-08 | Candidate primary P_crit is 1e-2 with 1e-3 sensitivity. | Matches the project plan, but must be frozen at G0. | `project_plan_v3_when_can_grid_reinforcement_wait.md` section 8E | HUMAN | proposed | -- |
| A-002 | 2026-07-08 | Candidate alpha grid is {0, 0.25, 0.5, 0.75, 1.0}. | Matches the project plan and agent guardrails, but must be frozen at G0/G1. | `project_plan_v3_when_can_grid_reinforcement_wait.md` section 3 | HUMAN | proposed | -- |
| A-003 | 2026-07-08 | Primary grid candidates are SimBench MV semi-urban and urban, with CIGRE MV as cross-check. | Matches the resolved design plan; final selection belongs to G0. | `project_plan_v3_when_can_grid_reinforcement_wait.md` section 8A | HUMAN | proposed | -- |
| A-004 | 2026-07-08 | Report alpha-indexed lower/upper probability bounds and MC CIs; never defuzzify to one scalar. | Hard reporting rule in the project plan and agent instructions. | `project_plan_v3_when_can_grid_reinforcement_wait.md` section 3; `agent_instructions.md` section 1 | all | proposed | -- |
| A-005 | 2026-07-09 | Parallel decision-transformer units are treated as one substation decision asset with aggregate apparent-power magnitude `abs(sum_i S_i(t)) / sum_i S_nom_i`, where `S_i(t)` is complex apparent power through unit `i`. This is equivalent to per-unit loading only under busbar-parallel operation with closed tie, equal taps, identical units, and no material circulating-current effects. If E1.S1 shows separate MV sections or open-tie operation, the decision asset must be redefined and escalated. | Matches the G0/G0-A1 decision-transformer aggregation rule and Tier-1 net-load summation evaluator. The magnitude is still direction-agnostic for screening/export reporting, while the primary event is conditioned on import direction. Avoids pretending the fast tier has per-unit split or circulating-current information. | G0 scope freeze; G0-A1; E1.S1 inventory follow-up must record element index(es), unit count, bus-tie configuration, and nameplates; G2 must compare aggregate loading against pandapower per-unit `res_trafo.loading_percent`. | A | proposed | -- |
| A-006 | 2026-07-09 | Constant nodal power factor supplies Q for `abs(S)` in Tier-1. The flexibility aggregator adjusts P, with Q following the assigned power factor. | The G0-A1 primary event uses apparent-power magnitude conditioned on import direction, while Tier-1 computes `S_net` from aggregated downstream net P and Q. This assumption makes the Q treatment explicit until later validation. | G0 scope freeze; G0-A1; E1.S4 Tier-1 evaluator; G2 summation-vs-AC comparison. | A | proposed | -- |
| A-007 | 2026-07-09 | Dutch KNMI weather drives German-measured SimBench baseline profiles. This is justified because heating load is modeled separately through the heat-pump layer, leaving baseline demand weakly weather-coupled. | G0 uses Dutch weather to keep adoption/weather/profile assumptions coherent with the Dutch scenario layer, while preserving validated SimBench topology and baseline profiles. | G0 scope freeze; E2.S1 data acquisition; E2.S3 heat-pump model. | C | proposed | -- |
| A-008 | 2026-07-09 | Fallback screen thresholds `L_base > 0.85 p.u.` and `L_2035^(rho=0) < 0.95 p.u.` are routing heuristics for grid selection, not scientific claims. | The thresholds decide whether the primary grid is too already-congested or too unthreatened to answer the deferral question. They must not be reported as planning standards or tuned silently for case interestingness. | G0 scope freeze; deterministic screen before Monte Carlo; G5 case selection. | A | proposed | -- |
| A-009 | 2026-07-10 | If the ElaadNL Laadprofielengenerator API does not accept native `simulated_year = 2033`, use the 2035 behavior library for 2033 and let `scenarios.yaml` nodal counts carry the 2033 volume. | Per-vehicle behavior drift from 2033 to 2035 is expected to be second-order compared with adoption counts, but the API-supported years must be checked before implementation. | `reports/elaad_profile_generation_spec.md`; EV-001. | C | proposed | -- |
| A-010 | 2026-07-10 | Primary EV charging capacities are 11 kW for home charging and 22 kW for public charging; note a 7.4 kW home sensitivity for later robustness if needed. | These are generator input conventions needed for deterministic profile generation and must be fixed before library creation. | `reports/elaad_profile_generation_spec.md`; ElaadNL generator documentation to verify during E2.S1 probe. | C | proposed | -- |
| A-011 | 2026-07-10 | ElaadNL generator `ev` profiles at `home` already represent only the home-share energy for that EV; nodal attachment scales by home-access EV count, not by an additional home-share factor. Public profiles scale by public charge-point count. | Prevents double-counting the generator's internal laadmix and keeps public/home scaling explicit in `scenarios.yaml`. | `reports/elaad_profile_generation_spec.md`; E2.S2/E3.S1 implementation tests. | C | proposed | -- |
| A-012 | 2026-07-10 | Sign convention for event direction: `P_net > 0` denotes net import from the upstream grid into the MV area; `P_net < 0` denotes net export/feed-in from the MV area to the upstream grid. Import and export loading both use apparent-power magnitude `abs(S_net) / S_nom,agg` after direction conditioning. | G0-A1 scopes the primary event to consumption-driven congestion while keeping export/feed-in loading visible as a side metric. The explicit sign convention prevents active-power-only loading or signed-import mistakes. | G0-A1; E1.S3 reverse-PV screen finding; E1.S4 evaluator implementation. | A | proposed | -- |
