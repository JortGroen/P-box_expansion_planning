# E5.S3 Real Endpoint-Count Manifest Guard

Status: synthetic/fail-closed preflight. This packet does not use real trajectories, estimate real `P(E)`, choose a capacity convention, sign A-013/G2/G3, approve a vertex shortcut, or produce manuscript numbers.

## What Is Ready

- `e5s3-real-output-error-endpoint-count-manifest-preflight-v1` validates the shape of future real output-error endpoint-count manifests before they can feed B-owned p-box/reporting surfaces.
- The preflight requires a declared alpha grid, lower/upper endpoint counts for every alpha row, common ordered sample IDs for CRN identity, and nested alpha-indexed probability bounds from the count estimator.
- Endpoint metadata must record G1-A2 loading-endpoint-before-event provenance, the exact lower/upper composition formulas, arbitrary unknown dependence, forbidden independent error sampling, forbidden probability widening, and the unwidened `P_net` direction gate.
- Real-use references must be clean signed/provenance IDs for A-013, G2, capacity convention/provenance, A-016 scenario consistency, and the real LoadingTrajectoryResult/output-error endpoint-count manifests.
- G0-A3 threshold metadata is executable in the packet: strict `L_import > 1.0 p.u.` for four consecutive 15-minute import steps over the full planning year.

## Fail-Closed Behavior

The packet always serializes `ready_for_real_use: false` today. Its blocker manifest names the missing upstream items rather than allowing a paper-facing p-box probability to be accepted from structural metadata alone.

## Rejected Bypasses

Synthetic tests cover tampering for missing IDs, proposed/unsigned references, scalar or defuzzified probability fields, alpha-grid mismatches, CRN/sample identity mismatches, post-hoc probability-margin widening, independent output-error sampling, widened direction gates, and non-primary threshold semantics.

## Remaining Inputs Needed

Real E5.S3 paper-facing use still needs real Agent A/C endpoint manifests plus signed G2 Tier-1 endpoints, signed A-013 grid-error value/form, approved capacity convention and denominator provenance, A-016 scenario consistency, and G3 if a vertex shortcut is claimed.
