# E3.S3 AC Materialization Fixture Scaffold - Agent A

## 2026-07-21 - scaffold

DID: Added a deterministic fixture path that materializes solved per-transformer AC P/Q result series into the existing AC `LoadingTrajectoryResult` boundary. The helper validates that the supplied transformer result series exactly match the denominator transformer indices, sums P and Q across those transformers, preserves 900-second calendar cadence, derives import/export/zero masks from unwidened aggregate P-net, and records manifestable materialization metadata alongside capacity and AC provenance.

VERIFIED: Focused AC evaluator tests passed with 9 tests after the materialization scaffold was added. Full ownership and project test results are recorded in the PR body after final validation.

OPEN: Scaffold only. No pandapower or TimeSeriesCPP validation batch, threshold-stratum test, held-out enclosure, E3.S2b, E4, event detection, `P(E)`, or Q-5-dependent analysis was run. The fixture uses synthetic/deterministic solved-result inputs only.

NEXT: Later G2 validation can connect real pandapower/TimeSeriesCPP solved transformer P/Q outputs to this materialization helper, then compare Tier-1 and AC trajectories through the runner after the required domain, denominator, and Q-5 dependencies are resolved.

SUGGESTED STATUS: `E3.S3 Tier-2 AC harness and Tier-1 enclosure | A | review | AC materialization fixture scaffold | E3.S2b domain screen, denominator decision, and Q-5 before threshold-stratum/G2 validation | PR: see PR body`

## 2026-07-21 - hardening follow-up

DID: Tightened transformer index validation so AC capacity metadata and transformer P/Q fixtures accept only exact nonnegative integer types. Python `int` and NumPy integer values are accepted; bool, float, string, and other non-integral values are rejected rather than coerced.

VERIFIED: Focused AC evaluator tests passed with 10 tests after adding regression coverage for invalid transformer indices and valid NumPy integer indices. Full validation is recorded in PR #64 after the final rerun.
