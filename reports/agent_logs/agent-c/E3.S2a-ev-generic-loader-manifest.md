## 2026-07-24 - E3.S2a - in review
DID: Added EV generic component-output manifests for the declared 2035 high/low/middle branches and a packet that exposes Agent A preflight inputs by scenario. Extended `data/get_ev_component_outputs.py` with a metadata-only `write-loader-manifests` mode and added EV tests proving the generic manifest remains fail-closed at Agent A's current one-node NPZ loader boundary without loading arrays.
VERIFIED: Focused EV tests passed: `./.venv/Scripts/python.exe -m pytest tests/test_ev_model.py -k "generic_loader or component_output"` -> 21 passed, 100 deselected. No API calls, held-out/quarantined access, integrated analysis, event/P(E), capacity screen, final branch selection, M-sufficiency claim, or manuscript number.
OPEN: Downstream IC-1/A-016/final-branch/held-out invocation/M-sufficiency blockers remain outside this PR. The ignored candidate EV NPZ outputs must still be present and checksum-verified in the consuming worktree, and `A-LOADER-MULTI-NODE-EV-OUTPUT-CONTRACT-NOT-YET-SIGNED` must be resolved before Agent A can load EV arrays.
NEXT: Agent A can use the packet for schema/checksum preflight bookkeeping only; real IC-1 EV loading still needs either an A-owned multi-node component-output loader contract or an EV-owned per-node export/manifest strategy.
