# E1.S4 G0-A3 Threshold Default Alignment

Task: E1.S4 Tier-1 evaluator follow-up.

Status: executable default alignment only. G0-A3 is approved and defines the
primary event as strict `L_import > 1.0 p.u.` for four consecutive 15-minute
import steps over the full planning year. This change updates the Tier-1
central default and focused tests to match that signed decision.

## Boundary

The strict `>` detector is unchanged. Persistent `1.1 p.u.` and `1.2 p.u.`
runs remain available only when callers pass those thresholds explicitly as
predeclared sensitivities. This packet does not run IC-1 net-load integration,
threshold screens, event analysis on real data, `P(E)`, capacity-screen analysis,
or manuscript-number generation.

## Verification

Focused tests cover the strict primary `1.0 p.u.` default and explicit `1.1` /
`1.2` sensitivity thresholds.
